<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_pressed="true">
        <shape>
            <!-- 高度40 -->
            <size android:height="30dp"/>
            <!-- 圆角弧度  -->
            <corners android:radius="7.5dp"/>
            <!-- 变化率 -->
            <gradient
                android:endColor="@color/main_dark_blue_fifteen"
                android:startColor="@color/main_dark_blue_fifteen" />
        </shape>
    </item>
    <item>
        <shape>
            <!-- 高度40 -->
            <size android:height="30dp"/>
            <!-- 圆角弧度  -->
            <corners android:radius="7.5dp"/>
            <!-- 变化率 -->
            <gradient
                android:endColor="@color/main_dark_blue"
                android:startColor="@color/main_dark_blue" />
        </shape>
    </item>
</selector>